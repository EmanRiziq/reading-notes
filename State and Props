# State and Props
### Based off the diagram, what happens first, the ‘render’ or the ‘componentDidMount’?
'render
'
### What is the very first thing to happen in the lifecycle of React?
Mounting
When an instance of a component is being created and inserted into the DOM it occurs during the mounting phase. Constructor, static getDerivedStateFromProps, render, componentDidMount, and UNSAFE_componentWillMount all occur in this order during mounting.

### Put the following things in the order that they happen: componentDidMount, render, constructor, componentWillUnmount, React Updates
1- constructor
2. render
3. React Updates
4. componentDidMount
5. componentWillUnmount


### What does componentDidMount do?
load anything using a network request or initialize the DOM, it should go here. This method is a good place to set up any subscriptions


### What types of things can you pass in the props?


### What is the big difference between props and state?
- propes is some thing passed into a componant, must be updatesd outside the componant
- state is handled inside the componant , you can update inside the componant


### When do we re-render our application?

### What are some examples of things that we could store in state?
counter and the things we need to change
